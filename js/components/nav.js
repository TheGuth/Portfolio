import React from 'react';


export const Nav = () => {
  return (
    <div>
      <ul>
        <li>Home</li>
        <li>Projects</li>
        <li>Contact</li>
      </ul>
    </div>
  );
}

// fixed position at top of screen when in desktop/landscape
// hamburger button when mobile

// all nav menu items will be linked to different parts of
// the page.
